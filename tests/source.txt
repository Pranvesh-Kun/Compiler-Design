# Variable declarations

int x = 5
float pi = 3.14
bool flag = true
string name = "Tony"

let inferred = 100

int defaultInt
float defaultFloat
bool defaultBool
string defaultString

array(int) numbers = [1,2,3,4,5]
array(string) names = ["luffy", "zoro"]
array(float) prices[10]

# Assignments

x = 10
x += 5
x -= 2
x *= 3
x /= 2
x %= 4

# Increment / decrement

x++
x--

# Index access and assignment

x = numbers[2]
numbers[0] = 999

# Unary expression

flag = not false

# Binary expressions

x = 1 + 2 * 3
flag = x > 5 and not false
flag = x <= 100 or x == 0

# Function calls

output(x)
output(numbers[0])
input(name)

# If / else if / else

if (x > 100) {
    output("big")
}
else if (x > 50) {
    output("medium")
}
else {
    output("small")
}

# While

while (x > 0) {
    x--
    if (x == 3) {
        continue
    }
    if (x == 1) {
        break
    }
}

# For

for (i in numbers) {
    output(i)
}

# Function declaration

int add(int a, int b) {
    return a + b
}

# Array parameter function

void printAll(array(int) arr) {
    for (i in arr) {
        output(i)
    }
}

# Function call inside expression

let answer = add(10, 20)

# Nested expression

let result = (x + 5) * (10 - 2) / 4

# Nested array literal

array(int) temp = [1,2,3]