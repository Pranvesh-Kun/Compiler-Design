# Interpreter
A custom interpreted programming language built from scratch in C++. This project implements a complete language pipeline:
Source code -> Lexer -> Parser -> Abstract Syntax Tree -> Interpreter (with inbuilt type checking)

Language Grammer / Current Features:

Data Types
